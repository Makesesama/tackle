# The directory Mix will write compiled artifacts to.
/_build/

# If you run "mix test --cover", coverage assets end up here.
/cover/

# The directory Mix downloads your dependencies sources to.
/deps/

# Package-local build output and dependencies for the separate Mix projects.
/packages/*/_build/
/packages/*/deps/
/plugins/*/_build/
/plugins/*/deps/
/frontends/*/_build/
/frontends/*/deps/

# tackle_web compiles assets with esbuild/tailwind into priv/static/assets.
/frontends/tackle_web/priv/static/assets/
/frontends/tackle_web/priv/static/cache_manifest.json
/frontends/tackle_web/assets/node_modules/

# Where third-party dependencies like ExDoc output generated docs.
/doc/

# Temporary files, for example, from tests.
/tmp/

# If the VM crashes, it generates a dump, let's ignore it too.
erl_crash.dump

# Also ignore archive artifacts (built via "mix archive.build").
*.ez

# Ignore package tarball (built via "mix hex.build").
tackle-*.tar

.pre-commit-config.yaml